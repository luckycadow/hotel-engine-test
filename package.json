{
  "name": "hotel-engine-test",
  "private": true,
  "version": "0.0.0",
  "description": "Hotel Engine test project",
  "repository": "https://github.com/luckycadow/hotel-engine-test",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^2.0.0",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0",
    "gulp-ng-annotate": "^2.0.0",
    "lite-server": "^2.1.0"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "gulp build && lite-server",
    "dev": "concurrently \"gulp watch\" \"lite-server\""
  }
}
